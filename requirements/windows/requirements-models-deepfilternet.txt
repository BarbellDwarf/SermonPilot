# DeepFilterNet model requirements (Windows)
# Install with: uv pip install -r requirements/windows/requirements-models-deepfilternet.txt
# Import in code as: import df  (not deepfilternet)

# Core audio processing
numpy>=1.26.2
soundfile>=0.12.1

# PyTorch ecosystem (single pin, same floor as pyproject)
torch>=2.13.0
torchaudio>=2.11.0

# DeepFilterNet model (imports as 'df')
deepfilternet>=0.5.6

# Optional: GPU acceleration for Windows
# torch==2.14.0+cu126; sys_platform == "win32"
# torchaudio==2.11.0+cu126; sys_platform == "win32"
