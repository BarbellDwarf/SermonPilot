# DeepFilterNet model requirements (Linux)
# Install with: uv pip install -r requirements/linux/requirements-models-deepfilternet.txt
# Import in code as: import df  (not deepfilternet)

# Core audio processing
numpy>=1.26.2
scipy>=1.11.4
soundfile>=0.12.1
librosa>=0.10.1

# PyTorch ecosystem (single pin, same floor as pyproject)
torch>=2.6.0
torchaudio>=2.6.0

# DeepFilterNet model (imports as 'df')
deepfilternet>=0.5.6

# Optional: GPU acceleration
# torch==2.6.0+cu124; sys_platform == "linux"
# torchaudio==2.6.0+cu124; sys_platform == "linux"
