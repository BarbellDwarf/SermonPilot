# CPU-only PyTorch (CPU index)
# Install with: uv pip install -r requirements/requirements-cpu.txt
# Overrides the torch family from requirements.txt with CPU-only builds.

--extra-index-url https://download.pytorch.org/whl/cpu

# Base runtime dependencies (mirror of pyproject.toml)
-r requirements.txt

# Per-index overrides for the torch family (single pin, same floor as pyproject)
torch==2.13.0+rocm7.1
torchaudio==2.6.0+cpu
