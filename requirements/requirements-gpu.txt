# GPU-enabled PyTorch (CUDA 12.4 index)
# Install with: uv pip install -r requirements/requirements-gpu.txt
# Overrides the torch family from requirements.txt with CUDA builds.

--extra-index-url https://download.pytorch.org/whl/cu124

# Base runtime dependencies (mirror of pyproject.toml)
-r requirements.txt

# Per-index overrides for the torch family (single pin, same floor as pyproject)
torch==2.13.0+rocm7.1
torchaudio==2.6.0+cu124
