# Development requirements (mirror of the [dev] extra in pyproject.toml)
# Install with: uv pip install -r requirements/requirements-dev.txt

# Base runtime dependencies (mirror of pyproject.toml)
-r requirements.txt

# Development tools
pytest>=7.0.0
black>=22.0.0
pylint>=2.15.0
ruff>=0.1.0

# Audacity integration
PyAudacity>=0.1.3

# Optional AI models (install manually due to complex dependencies):
# pip install voicefixer speechbrain demucs deepspeed
